<% content_for(:title, 'ブックマーク一覧 | RUNTEQ BOARD APP') %>
<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
      <%= search_form_for @q, url: bookmarks_boards_path do |f| %>
        <div class="input-group mb-3"><%= f.search_field :title_or_body_cont, class: 'form-control', placeholder: '検索ワード'%>
          <div class="input-group-append"><%=f.submit '検索', class:"btn btn-primary"%></div>
        </div>
      <% end %>
    </div>
  </div>
  <!-- 掲示板一覧 -->
  <% if @bookmark_boards.present?%>
  <div class="row">
    <div class="col-12">
      <div class="row">
        <%= render @bookmark_boards %>
      </div>
    </div>
  </div>
  <%= paginate @bookmark_boards,theme: 'bootstrap-5'%>
  <% else %>
    <p><%= t('.no_result')%></p>
  <% end %>
</div>